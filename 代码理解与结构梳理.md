# 多渠道告警系统 - 代码理解与结构梳理

> 文档版本：v1.0 | 更新日期：2026-05-12 | 面向对象：后续接手开发人员

---

## 一、整体架构概览

### 1.1 技术栈

**后端技术栈**：
- 框架：Spring Boot 2.7.18 + Java 8
- ORM：MyBatis-Plus 3.5.3.1
- 安全：Spring Security + JWT (jjwt 0.11.5)
- 缓存：Redis（实际使用有限）
- 数据库：MySQL 8.0
- 工具库：Hutool 5.8.22、Fastjson 2.0.40、Lombok

**前端技术栈**：
- 框架：Vue 3.4.21 + Vite 5.2.0
- UI组件：Element Plus 2.6.1
- 状态管理：Pinia 2.1.7
- 路由：Vue Router 4.3.0
- HTTP客户端：Axios 1.6.8

### 1.2 项目目录结构

```
├── backend/                          # Spring Boot 后端
│   ├── src/main/java/com/alert/
│   │   ├── AlertApplication.java     # 启动类 (@EnableScheduling)
│   │   ├── controller/               # 控制器层 (7个Controller)
│   │   ├── service/                  # 服务层
│   │   ├── entity/                   # 实体类 (7个实体)
│   │   ├── mapper/                   # MyBatis Mapper接口
│   │   ├── notify/                   # 通知策略 (策略模式)
│   │   ├── rule/                     # 规则引擎 (SpEL表达式)
│   │   ├── datasource/               # 数据源获取策略
│   │   ├── security/                 # JWT安全配置
│   │   ├── aop/                      # 操作日志切面
│   │   ├── config/                   # 配置类
│   │   ├── common/                   # 公共类
│   │   └── util/                     # 工具类
│   └── resources/
│       ├── application.yml           # 主配置
│       ├── application-local.yml     # 本地配置
│       └── schema.sql                # 数据库初始化脚本
│
├── frontend/                         # Vue 3 前端
│   ├── src/
│   │   ├── main.js                   # 入口文件
│   │   ├── views/                    # 页面组件 (9个页面)
│   │   ├── api/index.js              # API请求封装
│   │   ├── router/index.js           # 路由配置 + 守卫
│   │   └── stores/user.js            # Pinia用户状态管理
│   └── nginx.conf                    # 生产环境Nginx配置
│
└── docker-compose.yml                # 容器编排
```

---

## 二、核心业务链路

### 2.1 告警检查主流程

**入口**：`AlertCheckService.check()` - `@Scheduled(fixedDelay = 30000)` 每30秒执行一次

```
定时任务触发 (30秒/次)
    ↓
检查系统配置 alert.check.enabled
    ↓
获取所有启用的告警规则
    ↓ 遍历每条规则
    ├─ 解析数据源配置 (JSON → Map)
    ├─ 调用数据源获取数据 (DataSourceService.fetch)
    ├─ 规则引擎评估 (RuleEngine.evaluate)
    │   └─ SpEL表达式: #cpu > 80
    ├─ 触发告警
    │   ├─ 格式化通知消息 (简单字符串替换)
    │   ├─ 遍历通知渠道ID
    │   │   ├─ 获取解密后的渠道配置
    │   │   └─ 发送通知 (NotifyService.send)
    │   └─ 保存告警记录
    └─ 异常捕获 (单条规则失败不影响其他)
```

**核心代码位置**：`backend/src/main/java/com/alert/service/AlertCheckService.java:41-93`

---

## 三、鉴权与权限控制

### 3.1 JWT认证流程

**登录流程**：
1. 验证用户名密码（BCrypt匹配）
2. 检查用户状态（status=1）
3. 生成JWT Token，包含：userId、username、role
4. Token有效期：24小时

**请求认证** (`JwtAuthFilter`)：
1. 从Authorization头提取Bearer token
2. 验证token有效性
3. 解析出userId、username、role
4. 构建UserPrincipal，设置SecurityContext

### 3.2 权限控制方式

**后端两层防护**：

1. **URL级权限** (`SecurityConfig`)：
   - `/api/auth/**` 公开访问
   - OPTIONS请求放行
   - 其他请求需要认证

2. **方法级权限** (`@PreAuthorize("hasRole('ADMIN')")`)：
   - 用户管理、系统配置、操作日志等需要管理员权限

**前端权限控制**：

1. **路由守卫**：未登录跳转/login，管理员页面检查meta.admin
2. **按钮级别控制**：`v-if="userStore.isAdmin()"`

---

## 四、关键模块真实边界

### ⚠️ 4.1 规则引擎 - 实际可用边界

**代码位置**：`backend/src/main/java/com/alert/rule/RuleEngine.java`

**规则表达式真实情况**：

| 数据源类型 | 实际返回的key | 正确的表达式写法 |
|-----------|---------------|-----------------|
| **MOCK** | `cpu`, `memory`, `disk`, `responseTime` | `#cpu > 80`、`#responseTime > 3000` |
| **API** | API返回JSON的第一层key | 如API返回`{"value":85}`则写 `#value > 80` |

**❌ 已知Bug - schema.sql中的初始化数据不工作：**
- schema中写的表达式是 `value > 80`（没有`#`前缀）
- 但SpEL语法要求变量必须加 `#` 前缀
- 且MOCK数据源返回的是 `cpu`/`memory`，不是 `value`
- **结论：schema中预置的4条告警规则，没有一条能正常工作！**

**模板替换真实边界**：
- ❌ **不支持** `${ruleName}`、`${severity}` 等规则本身的属性
- ❌ schema中预置的模板 `${ruleName}: CPU使用率达到${value}%` 不会被正确替换
- ✅ 只替换数据源返回的key
- ❌ 不支持嵌套对象的点语法
- ❌ 不支持条件判断、循环

**实际能工作的模板示例**：
```
【告警】CPU使用率: ${cpu}%, 内存使用率: ${memory}%
```

### 4.2 通知策略模块 (策略模式)

**核心接口**：`NotifyStrategy`
```java
public interface NotifyStrategy {
    String getType();
    boolean send(Map<String, Object> config, String title, String content);
}
```

**已实现策略**：
- `DingTalkNotifyStrategy` - 钉钉机器人webhook（实际可用）
- `EmailNotifyStrategy` - 邮件（仅日志模拟，未实际实现）
- `SmsNotifyStrategy` - 短信（仅日志模拟）
- `WeComNotifyStrategy` - 企业微信（仅日志模拟）
- `FeishuNotifyStrategy` - 飞书（仅日志模拟）

**扩展方式**：实现接口 + @Component，自动注册。

### 4.3 数据源模块 (策略模式)

**已实现**：
- `ApiDataSourceFetcher` - HTTP API调用 (GET/POST)
- `MockDataSourceFetcher` - 随机模拟数据

### 4.4 操作日志模块 (AOP)

**使用方式**：`@OperationLog("操作描述")`

**记录内容**：操作人、操作类型、方法名、请求参数、IP、执行时长、状态、异常信息

---

## 五、配置与敏感信息 - 真实落地情况

### ⚠️ 5.1 动态配置 - 实际落地程度

**代码位置**：`backend/src/main/java/com/alert/service/SysConfigService.java`

| 功能 | 实际落地情况 | 备注 |
|------|-------------|------|
| 本地缓存 | ✅ ConcurrentHashMap | |
| Redis二级缓存 | ✅ 有实现 | 但Redis异常时**静默吞掉**，连日志都没有 |
| 单实例立即生效 | ✅ | |
| 分布式部署一致性 | ❌ **不支持** | 多实例时其他实例缓存不会更新 |
| 配置变更通知 | ❌ 没有实现 | |
| alert.check.thread.pool.size | ❌ 配置了但未使用 | 代码中无引用 |
| notify.retry.count | ❌ 配置了但未使用 | 代码中无引用 |
| 规则checkInterval字段 | ❌ 配置了但未使用 | 所有规则硬编码30秒周期 |

**分布式部署风险**：实例A更新配置 → 实例A本地缓存更新，但实例B/C还是旧值。

### 5.2 敏感信息处理 - 真实情况

| 敏感数据 | 处理方式 | 备注 |
|---------|---------|------|
| 通知渠道配置 | ✅ AES加密存储 | 列表返回`******`掩码 |
| 用户密码 | ✅ BCrypt哈希 | |
| 数据库密码 | ❌ 明文在application.yml | |
| Redis密码 | ❌ 明文在application.yml | |
| JWT密钥 | ❌ 明文在application.yml | 硬编码字符串 |
| AES加密密钥 | ❌ 明文在application.yml | 硬编码字符串 |
| 日志中的敏感数据 | ❌ 通知内容明文打日志 | EmailStrategy等直接log.info配置 |

**加密算法细节**：
- AES-128，密钥只取前16字节，多余字符被截断
- 固定密钥，无轮换机制

---

## 六、页面-接口-实体关系

### 6.1 页面路由

| 页面 | 路由 | 权限 | 对应实体 |
|------|------|------|----------|
| 登录页 | /login | - | User |
| 仪表盘 | /dashboard | 登录 | - |
| 告警规则 | /rules | 登录 | AlertRule |
| 通知渠道 | /channels | 登录 | NotifyChannel |
| 告警记录 | /records | 登录 | AlertRecord |
| 用户管理 | /users | 管理员 | User |
| 系统配置 | /configs | 管理员 | SysConfig |
| 操作日志 | /logs | 管理员 | SysLog |

### 6.2 核心实体关系

```
sys_user (用户)
    ├─ 1:N → alert_rule (created_by)
    ├─ 1:N → notify_channel (created_by)
    └─ 1:N → sys_log (user_id)

alert_rule (告警规则)
    └─ 1:N → alert_record (rule_id)
```

---

## 七、系统扩展点

### 7.1 新增通知渠道

只需实现 `NotifyStrategy` 接口：
```java
@Component
public class SlackNotifyStrategy implements NotifyStrategy {
    @Override
    public String getType() { return "SLACK"; }
    
    @Override
    public boolean send(Map<String, Object> config, String title, String content) {
        // 实现发送逻辑
        return true;
    }
}
```

### 7.2 新增数据源

实现 `DataSourceFetcher` 接口，同理自动注册。

---

## 八、关键依赖与风险点

### 8.1 关键依赖

| 依赖 | 版本 | 关注重点 |
|------|------|----------|
| Spring Boot | 2.7.18 | 已停止维护，考虑升级 |
| MyBatis-Plus | 3.5.3.1 | 性能优化 |
| Spring Security | 5.7.x | 配置正确性 |
| Hutool | 5.8.22 | HTTP调用超时 |
| Fastjson | 2.0.40 | 安全漏洞（v2相对安全） |

### ⚠️ 8.2 高优先级修复清单

1. **修复规则表达式不匹配问题**
   - schema中预置的规则表达式和模板都有问题，需要修正
   - 或者在RuleEngine中把rule的属性也放入context

2. **修复分布式配置一致性问题**
   - 加Redis发布订阅通知配置变更
   - 或者去掉本地缓存，直接读Redis

3. **实际使用checkInterval字段**
   - 目前所有规则硬编码30秒周期

4. **密钥外部化**
   - 不要硬编码在配置文件中，使用环境变量

5. **Redis异常不要静默吞掉**
   - 至少打个warn日志

### 8.3 功能缺失清单

- ❌ 无告警抑制/降噪机制（同一问题反复通知）
- ❌ 无通知失败重试机制
- ❌ 无告警聚合/合并
- ❌ 无分布式锁（多实例部署时定时任务重复执行）
- ❌ 无健康检查端点
- ❌ 无Prometheus监控指标
- ❌ 无单元测试
- ❌ 无接口文档（Swagger）

---

## 九、开发接手指南

### 9.1 快速启动

```bash
# Docker方式（推荐）
docker-compose up --build -d
# 访问 http://localhost:8081
# 账号 admin / admin123

# 本地开发
# 后端：启动 AlertApplication，使用 application-local.yml
# 前端：npm install && npm run dev
```

### 9.2 开发规范

1. **新增管理员接口**：添加 `@PreAuthorize("hasRole('ADMIN')")`
2. **记录操作日志**：添加 `@OperationLog("操作描述")`
3. **敏感数据存储**：调用 `EncryptUtil` 加密
4. **新增策略**：实现对应接口 + @Component 注解

### 9.3 本地调试Tips

- 测试规则时建议先用MOCK数据源
- 注意SpEL表达式变量要加 `#` 前缀
- 模板变量要和数据源返回的key一致
- 多实例部署时注意配置一致性问题

---

## 十、实际可工作功能清单

✅ JWT认证登录流程  
✅ 角色权限控制（ADMIN/USER）  
✅ 通知渠道AES加密存储  
✅ 操作日志AOP记录  
✅ 告警规则CRUD  
✅ 告警记录保存  
✅ 策略模式的通知渠道扩展机制  
✅ 策略模式的数据源扩展机制  
✅ 钉钉通知实际可用（webhook方式）  
✅ 前端路由守卫与权限控制

> **本文档基于代码实际实现编写，如有功能与文档不一致，以代码为准。**
