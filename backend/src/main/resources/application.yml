server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/alert_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: alert123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: redis
    port: 6379
    password: alert123456

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.alert.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: AlertSystemSecretKey2024VeryLongSecretKeyForJWTTokenGeneration
  expiration: 86400000

encrypt:
  key: AlertEncryptKey!

logging:
  level:
    com.alert: DEBUG
