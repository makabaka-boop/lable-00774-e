# 多渠道告警系统 - 代码理解与结构梳理

## 一、项目整体架构

### 1.1 技术栈概述

| 层级 | 技术选型 |
|------|---------|
| **前端** | Vue 3.4 + Vite 5.2 + Vue Router 4.3 + Pinia 2.1 + Element Plus 2.6 + Axios 1.6 |
| **后端** | Spring Boot 2.7.18 + MyBatis-Plus 3.5.3 + Spring Security + JWT (jjwt 0.11.5) |
| **数据库** | MySQL 8.0 + Redis |
| **工具库** | Hutool 5.8 + FastJSON 2.0 + Lombok |

### 1.2 目录结构总览

```
lable-00774-e/
├── backend/                          # 后端项目
│   ├── src/main/java/com/alert/
│   │   ├── aop/                      # AOP切面（操作日志）
│   │   ├── common/                   # 公共类（统一响应Result）
│   │   ├── config/                   # 配置类（MyBatis-Plus、全局异常）
│   │   ├── controller/               # REST控制器
│   │   ├── datasource/               # 数据源获取模块
│   │   ├── entity/                   # 数据库实体类
│   │   ├── mapper/                   # MyBatis Mapper
│   │   ├── notify/                   # 通知策略模块
│   │   ├── rule/                     # 规则引擎
│   │   ├── security/                 # 安全鉴权模块
│   │   ├── service/                  # 业务服务层
│   │   └── util/                     # 工具类（JWT、加密）
│   ├── src/main/resources/
│   │   ├── application.yml           # 主配置
│   │   ├── application-local.yml     # 本地开发配置
│   │   └── schema.sql                # Docker MySQL 首次启动初始化脚本
│   ├── Dockerfile
│   └── pom.xml
├── frontend/                         # 前端项目
│   ├── src/
│   │   ├── api/                      # API接口定义
│   │   ├── router/                   # 路由配置
│   │   ├── stores/                   # Pinia状态管理
│   │   ├── views/                    # 页面组件
│   │   ├── App.vue
│   │   ├── main.js
│   │   └── style.css
│   ├── Dockerfile
│   ├── nginx.conf
│   └── package.json
├── docker-compose.yml
└── README.md
```

---

## 二、后端核心架构详解

### 2.1 程序入口与核心配置

**主程序入口** `AlertApplication.java:8-14`

```java
@SpringBootApplication
@MapperScan("com.alert.mapper")
@EnableScheduling
public class AlertApplication {
    public static void main(String[] args) {
        SpringApplication.run(AlertApplication.class, args);
    }
}
```

**关键点**：
- `@EnableScheduling` 启用定时任务，用于自动执行告警检查
- `@MapperScan` 扫描 MyBatis Mapper 接口

### 2.2 安全鉴权模块

#### 2.2.1 安全配置 `SecurityConfig.java:19-58`

核心安全策略：
- **无状态会话**：`SessionCreationPolicy.STATELESS`，完全依赖 JWT
- **公开路径**：`/api/auth/**` 允许匿名访问（登录、注册）
- **JWT 过滤器**：在 `UsernamePasswordAuthenticationFilter` 之前执行
- **CORS 配置**：支持多个前端来源地址
- **方法级权限**：`@EnableGlobalMethodSecurity(prePostEnabled = true)`

#### 2.2.2 JWT 过滤器 `JwtAuthFilter.java:17-47`

工作流程：
1. 从请求头 `Authorization: Bearer <token>` 提取 Token
2. 使用 `JwtUtil.validateToken()` 验证 Token 有效性
3. 从 Token 中解析 `userId`、`username`、`role`
4. 构建 `UserPrincipal` 并设置到 `SecurityContextHolder`
5. 角色转换为 `ROLE_<role>` 格式（如 `ROLE_ADMIN`）

#### 2.2.3 JWT 工具 `JwtUtil.java:11-58`

Token 结构（Claims）：
- `sub`: 用户名
- `userId`: 用户ID
- `role`: 角色（ADMIN/USER）
- `iat`: 签发时间
- `exp`: 过期时间（默认 86400000ms = 24小时）

#### 2.2.4 权限控制方式

系统采用**双重权限控制**：

| 控制层级 | 实现方式 | 示例 |
|---------|---------|------|
| **路由级** | Spring Security 配置 | `SecurityConfig.java:34-37` 所有 `/api/**` 需认证 |
| **方法级** | `@PreAuthorize` 注解 | `AlertRuleController.java:39` `@PreAuthorize("hasRole('ADMIN')")` |

关键受保护接口（仅 ADMIN 可访问）：
- 创建/更新/删除告警规则
- 创建/更新/删除通知渠道
- 用户管理（CRUD）
- 系统配置管理
- 操作日志查看

### 2.3 实体类与数据库设计

#### 2.3.1 核心实体类

| 实体类 | 对应表 | 关键字段 |
|-------|-------|---------|
| `User` | `sys_user` | id, username, password(BCrypt), role(ADMIN/USER), status |
| `AlertRule` | `alert_rule` | dataSourceType, dataSourceConfig(JSON), ruleExpression(SpEL), channelIds |
| `NotifyChannel` | `notify_channel` | type, config(加密JSON) |
| `AlertRecord` | `alert_record` | ruleId, severity, notifyStatus, notifyResult |
| `SysLog` | `sys_log` | userId, operation, ip, duration, status |
| `SysConfig` | `sys_config` | configKey, configValue |

#### 2.3.2 数据库初始化 `schema.sql`

> **重要说明**：`schema.sql` 是 **Docker MySQL 容器首次启动的初始化脚本**，而非 Spring Boot 应用启动时执行。

从 `docker-compose.yml:14` 可见：
```yaml
volumes:
  - ./backend/src/main/resources/schema.sql:/docker-entrypoint-initdb.d/init.sql
```

脚本被挂载到 MySQL 容器的 `/docker-entrypoint-initdb.d/` 目录，MySQL 官方镜像在**首次启动且数据目录为空**时会自动执行该目录下的 `.sql` 文件。

**注意事项**：
- 容器重建但数据卷（`mysql_data`）已存在时，不会重新执行
- 后端应用本身不执行该 SQL，仅依赖数据库中已存在的表结构和数据

初始化内容：
- 默认管理员账号：`admin / admin123`
- 预置 4 个测试用户
- 3 条系统配置（告警检查开关、线程池大小、重试次数）
- 3 个示例通知渠道
- 4 条示例告警规则
- 5 条示例告警记录

### 2.4 核心业务流程

#### 2.4.1 告警检查与通知链路（核心）

**入口**：`AlertCheckService.java:41-54`

```java
@Scheduled(fixedDelay = 30000)  // 每30秒执行一次
public void check() {
    if (!"true".equals(sysConfigService.getConfig("alert.check.enabled"))) {
        return;
    }
    List<AlertRule> rules = alertRuleService.getActiveRules();
    for (AlertRule rule : rules) {
        checkRule(rule);
    }
}
```

**完整流程** `AlertCheckService.java:56-93`：

```
┌─────────────────────────────────────────────────────────────────┐
│                     AlertCheckService.checkRule()              │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
          ┌───────────────────────────────────────────┐
          │  1. 解析数据源配置 (JSON → Map)           │
          │     dataSourceConfig 字段                 │
          └───────────────────────────────────────────┘
                                  │
                                  ▼
          ┌───────────────────────────────────────────┐
          │  2. 数据获取 DataSourceService.fetch()    │
          │     ├─ 根据 type 选择 DataSourceFetcher   │
          │     └─ 返回 Map<String, Object>           │
          └───────────────────────────────────────────┘
                                  │
                                  ▼
          ┌───────────────────────────────────────────┐
          │  3. 规则评估 RuleEngine.evaluate()        │
          │     ├─ 创建 StandardEvaluationContext     │
          │     ├─ 注入数据变量                        │
          │     └─ 执行 SpEL 表达式 (返回 boolean)    │
          └───────────────────────────────────────────┘
                                  │
                     ┌────────────┴────────────┐
                     │ 表达式为 false          │ 表达式为 true
                     ▼                         ▼
                【跳过】           ┌──────────────────────────────┐
                                 │  4. 格式化通知消息             │
                                 │     RuleEngine.formatMessage()│
                                 │     替换 ${key} 变量           │
                                 └──────────────────────────────┘
                                            │
                                            ▼
                          ┌─────────────────────────────────┐
                          │  5. 遍历 channelIds             │
                          │     拆分逗号分隔的ID列表         │
                          └─────────────────────────────────┘
                                            │
                                            ▼
                          ┌─────────────────────────────────┐
                          │  6. 获取解密后的渠道配置         │
                          │     notifyChannelService.       │
                          │     getDecrypted(id)            │
                          └─────────────────────────────────┘
                                            │
                                            ▼
                          ┌─────────────────────────────────┐
                          │  7. 发送通知 NotifyService.send()│
                          │     ├─ 按 type 选择策略          │
                          │     ├─ 解密 config 字段          │
                          │     └─ 调用 NotifyStrategy       │
                          └─────────────────────────────────┘
                                            │
                                            ▼
                          ┌─────────────────────────────────┐
                          │  8. 保存告警记录                 │
                          │     AlertRecord (含通知结果)    │
                          └─────────────────────────────────┘
```

#### 2.4.2 规则引擎 `RuleEngine.java:12-38`

**表达式评估**：使用 Spring EL (SpEL)
- 示例表达式：`value > 80`、`status != "UP"`
- 数据注入：将数据源返回的 Map 作为变量注入上下文
- 返回值：`Boolean`，true 表示触发告警

**消息模板**：简单的字符串替换
- 模板格式：`【告警】${ruleName}: CPU使用率达到${value}%`
- 支持 `${key}` 占位符，从数据源 Map 中取值

#### 2.4.3 数据源获取模块 `DataSourceService.java:11-33`

**设计模式**：策略模式 + Spring 自动注册
- 接口：`DataSourceFetcher`（定义 `getType()` 和 `fetch()`）
- 注册：`@PostConstruct` 时扫描所有实现类，按 type 存入 Map
- 调度：根据 `AlertRule.dataSourceType` 选择对应 Fetcher

**已实现的数据源**：

| 类型 | 实现类 | 说明 |
|-----|-------|------|
| `API` | `ApiDataSourceFetcher` | 发送 HTTP GET/POST 请求，解析 JSON 响应 |
| `MOCK` | `MockDataSourceFetcher` | 生成随机测试数据（CPU、内存、磁盘等） |

**扩展点**：只需新增类实现 `DataSourceFetcher` 接口，添加 `@Component` 注解即可自动注册。

#### 2.4.4 通知分发模块 `NotifyService.java:14-40`

**设计模式**：策略模式（与数据源模块一致）
- 接口：`NotifyStrategy`
- 配置解密：使用 `EncryptUtil.decrypt()` 解密渠道配置

**通知策略实现状态**：

| 类型 | 实现类 | 实际实现 | 状态 |
|-----|-------|---------|------|
| **DINGTALK** | `DingTalkNotifyStrategy` | 真实 HTTP POST 调用钉钉 Webhook | ✅ 真实实现 |
| **WECOM** | `WeComNotifyStrategy` | 真实 HTTP POST 调用企业微信 Webhook | ✅ 真实实现 |
| **FEISHU** | `FeishuNotifyStrategy` | 真实 HTTP POST 调用飞书 Webhook | ✅ 真实实现 |
| **EMAIL** | `EmailNotifyStrategy` | 仅 `log.info` 打印日志，无实际邮件发送 | ⚠️ 模拟实现 |
| **SMS** | `SmsNotifyStrategy` | 仅 `log.info` 打印日志，无实际短信发送 | ⚠️ 模拟实现 |

**EmailNotifyStrategy 源码**（仅日志）：
```java
@Override
public boolean send(Map<String, Object> config, String title, String content) {
    // 实际项目中使用JavaMailSender发送邮件
    String host = (String) config.get("host");
    String to = (String) config.get("to");
    log.info("发送邮件通知 -> host:{}, to:{}, title:{}, content:{}", host, to, title, content);
    return true;
}
```

**扩展点**：新增通知渠道只需实现 `NotifyStrategy` 接口。

### 2.5 敏感信息处理

#### 2.5.1 加密工具 `EncryptUtil.java:9-35`

- **算法**：AES 对称加密（Hutool 库）
- **密钥**：从配置 `encrypt.key` 读取，截取前 16 字节
- **存储格式**：Hex 编码字符串
- **容错处理**：解密失败时直接返回原文（`EncryptUtil.java:28-35`）

#### 2.5.2 应用场景

**1. 通知渠道配置** `NotifyChannelService.java:19-24`

> **注意**：通知渠道配置的加密存在**不一致性**

| 场景 | 加密状态 | 说明 |
|------|---------|------|
| **新创建/更新** | ✅ 加密 | `saveChannel()` 调用 `encryptUtil.encrypt()` |
| **初始化数据** | ❌ 明文 | `schema.sql:106-110` 中直接写入明文 JSON |

**schema.sql 中的初始化数据（明文）**：
```sql
INSERT INTO notify_channel (name, type, config, status, created_by) VALUES
('企业邮箱', 'EMAIL', '{"host":"smtp.company.com","port":465,"username":"alert@company.com","password":"xxx","ssl":true}', 1, 1),
('钉钉告警群', 'DINGTALK', '{"webhook":"https://oapi.dingtalk.com/robot/send?access_token=xxx","secret":"xxx"}', 1, 1),
('飞书告警群', 'FEISHU', '{"webhook":"https://open.feishu.cn/open-apis/bot/v2/hook/xxx"}', 1, 1)
```

- 返回前端时脱敏：`channel.setConfig("******")`
- 内部使用时解密：`getDecrypted(id)` 方法

**2. 用户密码** `UserService.java:24-38`
- 使用 Spring Security 的 `BCryptPasswordEncoder`
- 登录时：`passwordEncoder.matches(rawPassword, encodedPassword)`
- 注册时：`passwordEncoder.encode(rawPassword)`

**3. JWT 签名** `JwtUtil.java:13-22`
- 使用配置的 `jwt.secret` 进行 HMAC-SHA 签名

### 2.6 操作日志 `LogAspect.java:20-88`

**实现方式**：Spring AOP 环绕通知
- 注解：`@OperationLog("操作描述")`
- 切面：`@Around("@annotation(operationLog)")`

**记录内容**：
- 用户信息（userId, username）
- 操作描述、方法名
- 请求参数（JSON 序列化）
- IP 地址（支持 X-Forwarded-For、X-Real-IP）
- 执行时长、状态（成功/失败）
- 异常信息（如有）

---

## 三、前端架构详解

### 3.1 入口与依赖注入 `main.js:1-20`

```javascript
const app = createApp(App)
app.use(createPinia())      // 状态管理
app.use(router)             // 路由
app.use(ElementPlus, { locale: zhCn })  // UI 组件库
app.mount('#app')
```

### 3.2 路由配置 `router/index.js:1-43`

#### 路由结构

```
/login                     # 登录页
/                          # 主布局（Layout）
├── /dashboard             # 仪表盘
├── /rules                 # 告警规则
├── /channels              # 通知渠道
├── /records               # 告警记录
├── /users                 # 用户管理（仅管理员）
├── /configs               # 系统配置（仅管理员）
└── /logs                  # 操作日志（仅管理员）
```

#### 路由守卫 `router/index.js:27-43`

```javascript
router.beforeEach(async (to, from, next) => {
  // 1. 未登录且访问非登录页 → 跳转登录
  // 2. 已登录且访问登录页 → 跳转首页
  // 3. 已登录但无用户信息 → 调用 fetchUser()
  // 4. 管理员页面但非管理员 → 跳转首页
})
```

### 3.3 API 层 `api/index.js:1-78`

#### Axios 实例配置

- **Base URL**：`/api`（通过 Nginx 反向代理到后端）
- **超时**：10 秒

#### 请求拦截器 `api/index.js:10-16`
```javascript
// 自动注入 Token: Authorization: Bearer <token>
const token = localStorage.getItem('token')
if (token) {
  config.headers.Authorization = `Bearer ${token}`
}
```

#### 响应拦截器 `api/index.js:18-38`
```javascript
// 1. 业务码非 200 → 弹出错误提示并 reject
// 2. 401 未授权 → 清除 Token + 跳转登录
// 3. 403 无权限 → 弹出提示
// 4. 其他错误 → 弹出网络错误
```

### 3.4 状态管理 `stores/user.js:1-35`

**Pinia Store** 管理用户状态：
- `token`: 从 localStorage 读取/持久化
- `user`: 当前用户信息（包含 role）
- `login()`: 登录并保存 Token
- `logout()`: 登出并清除状态
- `fetchUser()`: 根据 Token 获取用户详情
- `isAdmin()`: 判断是否为管理员

### 3.5 核心页面

| 页面 | 文件 | 功能 |
|-----|------|------|
| 登录页 | `Login.vue` | 用户名密码登录，默认 admin/admin123 |
| 主布局 | `Layout.vue` | 侧边栏菜单、面包屑、用户信息、路由出口 |
| 告警规则 | `Rules.vue` | 规则 CRUD、手动触发检查 |
| 通知渠道 | `Channels.vue` | 渠道 CRUD（配置加密存储） |
| 告警记录 | `Records.vue` | 查看历史告警、通知状态 |
| 用户管理 | `Users.vue` | 用户 CRUD、状态切换、角色分配 |
| 系统配置 | `Configs.vue` | 动态配置项管理 |
| 操作日志 | `Logs.vue` | 审计日志查询 |

---

## 四、前后端接口调用链路

### 4.1 登录流程

```
前端 Login.vue
    │  submit
    ▼
userStore.login(username, password)
    │  调用 api.login()
    ▼
api.post('/api/auth/login', data)
    │  请求拦截器注入 Token（此时无 Token）
    ▼
后端 AuthController.login()
    │
    ├─ UserService.login()
    │   ├─ 查询用户
    │   ├─ BCrypt 密码校验
    │   ├─ 检查账号状态
    │   └─ JwtUtil.generateToken()
    │
    └─ 返回 Result.success({ token, user })
    │
    ▼
前端响应拦截器（code=200）
    │
    ▼
userStore: 保存 token 到 localStorage 和 store
    │
    ▼
router.push('/')
    │
    ▼
Layout.vue + Dashboard.vue
```

### 4.2 调用受保护接口（以创建告警规则为例）

```
前端 Rules.vue
    │  提交表单
    ▼
api.createRule(data)
    │  请求拦截器
    │  ├─ 从 localStorage 读取 Token
    │  └─ 注入 Header: Authorization: Bearer <token>
    ▼
后端 AlertRuleController.create()
    │
    ├─ JwtAuthFilter.doFilterInternal()
    │   ├─ 验证 Token
    │   ├─ 解析 userId/username/role
    │   └─ 设置 SecurityContext
    │
    ├─ @PreAuthorize("hasRole('ADMIN')") 权限检查
    │
    ├─ @OperationLog AOP 环绕
    │   ├─ 记录操作日志开始
    │   ├─ proceed() 执行业务
    │   └─ 保存操作日志（含执行时长）
    │
    ├─ AlertRuleService.save()
    │   └─ MyBatis-Plus insert
    │
    └─ 返回 Result.success(rule)
```

---

## 五、系统扩展点

### 5.1 新增数据源类型
```java
// 1. 新增 Fetcher 类
@Component
public class PrometheusDataSourceFetcher implements DataSourceFetcher {
    @Override
    public String getType() {
        return "PROMETHEUS";  // 新增类型标识
    }
    @Override
    public Map<String, Object> fetch(Map<String, Object> config) {
        // 实现 Prometheus 查询逻辑
    }
}

// 2. 无需修改其他代码，自动注册到 DataSourceService
```

### 5.2 新增通知渠道
```java
// 1. 新增 Strategy 类
@Component
public class SlackNotifyStrategy implements NotifyStrategy {
    @Override
    public String getType() {
        return "SLACK";
    }
    @Override
    public boolean send(Map<String, Object> config, String title, String content) {
        // 实现 Slack Webhook 调用
    }
}

// 2. 前端新增对应类型选项即可
```

### 5.3 新增权限角色
1. 数据库 `sys_user.role` 字段新增角色值
2. 前端 `userStore.isAdmin()` 扩展判断逻辑
3. 后端 `@PreAuthorize` 注解使用新角色

### 5.4 自定义规则函数
在 `RuleEngine` 中扩展 SpEL 上下文，注入自定义工具类：
```java
context.setVariable("util", myRuleUtil);
// 表达式中可使用: util.customFunction(value)
```

---

## 六、关键依赖清单

| 依赖 | 版本 | 用途 |
|-----|------|------|
| Spring Boot | 2.7.18 | 应用框架 |
| Spring Security | - | 安全认证 |
| MyBatis-Plus | 3.5.3.1 | ORM 框架 |
| MySQL Connector | 8.0.33 | 数据库驱动 |
| JJWT | 0.11.5 | JWT 生成与解析 |
| Hutool | 5.8.22 | AES 加密、HTTP 工具 |
| FastJSON | 2.0.40 | JSON 序列化 |
| Redis Starter | - | Redis 集成（当前配置但未实际使用） |
| Vue | 3.4.21 | 前端框架 |
| Element Plus | 2.6.1 | UI 组件库 |
| Pinia | 2.1.7 | 状态管理 |
| Axios | 1.6.8 | HTTP 客户端 |

---

## 七、风险点与改进建议

### 7.1 🔴 高风险

**1. 密钥硬编码在配置文件**
- `application.yml:22-27` 中 `jwt.secret` 和 `encrypt.key` 明文存储
- **风险**：代码仓库泄露导致密钥泄露
- **建议**：使用环境变量、配置中心（Nacos/Apollo）或 Vault 管理敏感配置

**2. 数据库密码明文配置**
- `application.yml:5-9` 数据库账号密码明文
- **建议**：同上，使用环境变量或加密配置

**3. SpEL 表达式注入风险**
- `RuleEngine.java:17-29` 用户输入的表达式直接执行
- **风险**：恶意表达式可能执行任意代码或访问敏感对象
- **建议**：
  - 限制 SpEL 上下文可用的类和方法
  - 对表达式进行语法检查和白名单校验
  - 仅允许管理员配置规则

**4. 缺少通知发送限流**
- 告警规则可能频繁触发，导致通知轰炸
- **建议**：增加告警抑制（suppression）、告警聚合（aggregation）机制

### 7.2 🟡 中风险

**5. 数据源 API 无认证支持**
- `ApiDataSourceFetcher.java:19-29` 仅支持简单 GET/POST，无 Header/Token 配置
- **建议**：扩展 `dataSourceConfig` 支持认证配置

**6. 无告警抑制/去重**
- 同一规则每次检查触发都会发送通知
- **建议**：实现静默期（silence）、重复告警合并

**7. 操作日志记录敏感参数**
- `LogAspect.java:55-58` 直接序列化所有参数，可能包含密码等敏感信息
- **建议**：增加敏感字段脱敏逻辑

**8. Redis 配置但未使用**
- `pom.xml` 引入了 Redis Starter，但代码中无实际使用
- **建议**：要么移除依赖，要么用 Redis 实现：
  - 分布式锁（防止多实例重复检查）
  - 告警状态缓存
  - Token 黑名单

**9. 通知渠道配置加密不一致**
- `schema.sql` 中初始化数据为明文，新建/更新时加密
- **建议**：初始化脚本也写入加密后的数据，或在应用启动时检测并加密

### 7.3 🟢 低风险/改进点

**10. 定时任务单线程执行**
- `@Scheduled` 默认单线程，规则数量多会累积延迟
- **建议**：配置 `TaskScheduler` 线程池，或使用异步执行

**11. 缺少单元测试**
- 项目中未见测试代码
- **建议**：补充核心逻辑的单元测试（规则引擎、通知策略）

**12. 通知策略缺少重试机制**
- 发送失败直接返回 false，无重试
- **建议**：结合系统配置 `notify.retry.count` 实现重试

**13. 缺少告警通知状态回调**
- 无法获知通知渠道的实际送达状态
- **建议**：对于支持回调的渠道（如钉钉），实现回执处理

**14. 前端路由权限仅前端校验**
- 前端路由守卫可通过浏览器调试绕过
- **建议**：后端接口已做权限校验，风险较低；但可增加前端动态路由

**15. EMAIL/SMS 策略未真正实现**
- 仅打印日志，无实际发送能力
- **建议**：
  - EMAIL：集成 Spring Boot Mail Starter + JavaMailSender
  - SMS：集成阿里云/腾讯云短信 SDK

---

## 八、部署说明

### 8.1 Docker Compose 部署

项目根目录 `docker-compose.yml` 包含：
- `mysql`: MySQL 8.0（端口 3307）
- `redis`: Redis（配置但未实际使用）
- `backend`: Spring Boot 应用（端口 8080）
- `frontend`: Nginx 托管的 Vue 应用（端口 8081）

### 8.2 本地开发

后端：
```bash
# 设置 profile 连接本地 MySQL/Redis
SPRING_PROFILES_ACTIVE=local mvn spring-boot:run
```

前端：
```bash
npm run dev   # 启动在 http://localhost:5173
```

### 8.3 默认账号
- **用户名**：`admin`
- **密码**：`admin123`

---

## 九、核心类关系图（简化）

```
┌─────────────────────────────────────────────────────────────┐
│                         前端层                                │
├─────────────────────────────────────────────────────────────┤
│  Views.vue  →  Pinia Store  →  API Module  →  Axios         │
└─────────────────────────────────┬───────────────────────────┘
                                  │ HTTP (JWT in Header)
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│                        后端 Controller 层                     │
├─────────────────────────────────────────────────────────────┤
│  AuthController / AlertRuleController / NotifyChannel...    │
│  (方法级 @PreAuthorize + @OperationLog AOP)                  │
└─────────────────────────────────┬───────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│                          Service 层                           │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │ UserService      │  │ AlertCheckService │──────────────┐ │
│  │ (登录/注册)       │  │ (定时调度)        │              │ │
│  └──────────────────┘  └────────┬─────────┘              │ │
│                                   │                        │ │
│  ┌──────────────────┐  ┌─────────▼─────────┐  ┌────────▼▼┐ │
│  │ NotifyChannel    │  │ DataSourceService │  │ RuleEngine│ │
│  │ Service(加密)    │  │ (策略模式)         │  │ (SpEL)    │ │
│  └──────────────────┘  └────────┬─────────┘  └───────────┘ │
│                                   │                        │ │
│  ┌──────────────────┐  ┌─────────▼─────────┐              │ │
│  │ NotifyService    │  │ DataSourceFetcher │              │ │
│  │ (策略模式)        │  │ 实现类             │              │ │
│  └────────┬─────────┘  └───────────────────┘              │ │
│           │                                               │ │
│  ┌────────▼─────────┐                                    │ │
│  │ NotifyStrategy   │                                    │ │
│  │ 实现类             │                                    │ │
│  └──────────────────┘                                    │ │
└───────────────────────────┬───────────────────────────────┘ │
                            │                                 │
                            ▼                                 ▼
┌─────────────────────────────────────────────────────────────┐
│                       Mapper 层 (MyBatis-Plus)               │
├─────────────────────────────────────────────────────────────┤
│  UserMapper / AlertRuleMapper / NotifyChannelMapper ...    │
└─────────────────────────────────┬───────────────────────────┘
                                  │
                                  ▼
                        ┌──────────────────┐
                        │   MySQL 数据库    │
                        └──────────────────┘
```

---

## 十、后续开发建议

### 10.1 近期优先
1. **密钥安全**：将敏感配置移至环境变量
2. **SpEL 安全**：限制表达式能力
3. **告警抑制**：防止通知轰炸
4. **配置加密一致性**：初始化数据也使用加密存储
5. **补充测试**：核心逻辑单元测试

### 10.2 中期规划
1. **Redis 落地**：分布式锁 + Token 黑名单
2. **多实例部署**：解决定时任务重复执行问题
3. **通知重试**：失败通知自动重试
4. **告警聚合**：同类告警合并发送
5. **EMAIL/SMS 真实实现**：集成邮件服务器和短信服务商

### 10.3 长期演进
1. **规则可视化**：拖拽式规则编辑器
2. **告警升级**：未响应告警自动升级
3. **监控集成**：Prometheus/Grafana 支持
4. **告警分析**：历史告警趋势分析
